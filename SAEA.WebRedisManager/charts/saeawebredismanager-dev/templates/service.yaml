apiVersion: v1
kind: Service
metadata:
  name: {{ template "saeawebredismanager.fullname" . }}
  labels:
    app: {{ template "saeawebredismanager.name" . }}
    chart: {{ template "saeawebredismanager.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "saeawebredismanager.name" . }}
    release: {{ .Release.Name }}
